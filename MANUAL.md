# 新人後台操作手冊

嗨，恭喜你們！

這份手冊是給你們自己用的。網站已經幫你們做好了，
接下來要放什麼內容、要開哪些功能，都在**新人後台**裡自己決定。

不用學什麼技術，也不會不小心弄壞什麼 ——
後台改不動的東西一律改不動，改得動的東西都可以改回來。
放心點下去就好。

有任何一步卡住，直接找我們，不要自己跟它耗。

---

## 目錄

- [開始之前](#開始之前)
- [第一件事：把後台放到手機主畫面](#第一件事把後台放到手機主畫面)
- [後台長什麼樣子](#後台長什麼樣子)
- [出席回覆](#出席回覆)
- [悄悄話](#悄悄話)
- [婚禮資訊](#婚禮資訊)
- [桌次](#桌次)
- [排桌管理](#排桌管理)
- [收禮小幫手](#收禮小幫手)
- [感謝信](#感謝信)
- [婚禮小卡](#婚禮小卡)
- [新人故事牆](#新人故事牆)
- [新人熟悉測驗](#新人熟悉測驗)
- [賓客那邊會看到什麼](#賓客那邊會看到什麼)
- [婚禮前・當天・結束後](#婚禮前當天結束後)
- [想改但改不動的東西](#想改但改不動的東西)
- [常見問題](#常見問題)

---

## 開始之前

### 你手上會有兩個網址

| 網址 | 誰用 | 做什麼 |
|---|---|---|
| `.../w/你們的代號/` | 賓客 | 婚禮網站本人，發出去給大家看的 |
| `.../w/你們的代號/admin` | 只有你們 | 後台，所有內容從這裡改 |

後台網址**不會出現在網站上的任何地方**，賓客點不到、Google 也搜不到。

### 怎麼登入

打開後台網址 → 按「**用 Google 帳號登入**」→ 選你們的 Google 帳號，結束。

沒有密碼要記，也沒有帳號要辦。我們事先把你們的 Gmail 加進名單裡，
名單以外的帳號就算打開這一頁也一個字都改不了 —— 連結不小心傳出去也不會出事。

> 登入的信箱要用**我們登記的那一組**。換了一個 Google 帳號會進不去，
> 想加另一半、加伴娘的帳號，跟我們說一聲就好。

### 一個習慣就好：改完記得按「儲存」

大部分分頁的內容，**改完要按下方那顆儲存按鈕**才算數
（例如「儲存婚禮資訊」「儲存流程」「儲存表單設定」）。
按錯了、改壞了、想反悔，旁邊都有一顆「**還原成目前的內容**」把畫面救回來。

少數幾個地方是**改完自動存**，會另外標出來（婚禮小卡的卡名、開關類的東西）。

存好之後，賓客那邊重新整理網頁就看得到，不用等、不用通知我們。

---

## 第一件事：把後台放到手機主畫面

後台在手機上是完整可用的，桌次、收禮、看回覆都在手機上做得到。

先把它放到主畫面 —— 之後就像一個 App，點一下打開，
不用每次翻書籤、也不用再找那串長長的網址。

### iPhone / iPad

1. 用 **Safari** 打開後台網址（這一步要用 Safari，其他瀏覽器沒有這個選單）
2. 先登入一次
3. 按畫面**下方中間的「分享」鍵**（一個方框往上的箭頭）
4. 往下滑，找到「**加入主畫面**」
5. 名稱改成看得懂的，例如「**婚禮後台**」
6. 右上角「**加入**」

### Android

1. 用 **Chrome** 打開後台網址
2. 先登入一次
3. 按右上角的「**⋮**」
4. 選「**加到主畫面**」或「**安裝應用程式**」（看手機版本，兩個都對）
5. 名稱改成「**婚禮後台**」，按「**新增**」

### 小技巧：直接停在你最常用的那一頁

後台的網址會跟著你現在看的分頁變 ——
所以你可以**先切到想要的分頁，再加到主畫面**，之後一點開就直接是那一頁。

婚禮當天最好用的兩個：

| 想一點開就看到 | 先切到 | 網址結尾會變成 |
|---|---|---|
| 收禮金額即時統計 | 收禮小幫手 → 收禮統計 | `#butler/stats` |
| 出席回覆名單 | 出席回覆 → 回覆資訊 | `#rsvp/replies` |

兩個都想要？加兩次，取不同名字就好。

> 建議兩個人的手機**都加一次**。婚禮當天你們不會待在一起。

---

## 後台長什麼樣子

### 最上面那一列

- 左邊寫著「新人後台」和你登入的帳號
- 右邊「**查看網站**」：直接打開賓客看到的樣子，改完想確認就按它
- 右邊「**登出**」

### 左邊的選單

電腦上左邊固定一排分頁；**手機上收起來了**，按左上角的「**≡**」打開。

分頁只會出現**你們這場有開的功能**。
沒看到「排桌管理」或「收禮小幫手」是正常的 —— 這兩個要另外開，跟我們說一聲就好。

### 有些分頁裡面還有小分頁

例如「出席回覆」點進去，上面還有一排「出席回覆總覽 / 回覆資訊 / 表單設定」。
下面各章節會一個一個講。

---

## 出席回覆

賓客填的邀請函回覆，全部收在這裡。四個小分頁。

### 出席回覆總覽

一進來最大那個數字是**總回覆人數**，底下小字補上確定出席幾位。

再往下是五張圓圈圖，一張對一個問題：出席、飲食、兒童座椅、喜帖、喜餅。
想知道「素食要準備幾份」「兒童椅要跟飯店要幾張」，看這裡最快。

> 「飲食」那一張算的是**人**（每筆回覆裡的葷素加起來），
> 其他四張算的是**回覆筆數**。這是刻意的 —— 葷素是分到每個人身上的。

### 回覆資訊

完整的名單，一列一位賓客，一欄一件事。欄位很多，可以左右滑，
表頭會固定在上面，滑到第 40 列還看得出自己在看哪一欄。

上面那排是篩選：

- **出席**：全部 / 熱情出席 / 視情況而定 / 無法出席
- **標籤**：依你分好的類別篩（有開標籤功能才有）
- **搜尋**：名字、留言、備註都找得到

右上角「**匯出 CSV**」把名單存成檔案，Excel 開得起來，中文不會亂碼。
**匯出的是你現在篩選出來的那些**，不是全部 ——
想給飯店一份素食名單，就先篩再匯出。

**同一個人重複送出好幾次？** 每一列右邊有「刪除」，把多的那幾筆拿掉就好。
會問兩次（第二次要打字），避免手滑。

> **回覆的內容改不動，這是故意的。** 那是賓客親手送出的紀錄，
> 留著原文，日後對帳、對餐才不會有兩套說法。真的要更正就整筆刪掉請他重填。

### 表單設定

決定賓客在邀請函那一頁**被問什麼**。右上角「**查看表單**」直接打開他們看到的樣子。

- **題目**：由上到下就是賓客看到的順序。灰灰的、打勾但點不動的是固定題目，每份回覆都要有。
  可以自己關掉的有：喜帖發送方式、喜餅領取方式、想對新人說的話。
- **聯絡方式**：電話 / LINE ID / Email，**可以複選**。
  勾了幾種，賓客就要至少填其中一種；三種都不勾就整題不問。
- **表單資訊**：邀請函上除了題目以外還會出現什麼 ——
  婚禮資訊（在「婚禮資訊」分頁填，這裡只是列給你看）、兩人的故事、照片集。

改完按「**儲存表單設定**」。

> **關掉的題目不會弄丟已經收到的回覆**，只是名單和匯出檔裡那一欄留白。
> 隨時可以再打開。

### 設定賓客標籤

把賓客分群用的，例如：VIP、長輩、小孩、行動不便、大學同學、公司同事、親戚。
按「**加入常用標籤**」一次把常見的都帶進來，也可以自己新增。

**一位賓客可以有好幾個標籤。**

- 勾「**當表單選項**」：賓客填表時可以自己選（「更具體是哪一種？」那一題）
- 沒勾的：只有你們在後台看得到、自己掛上去

掛標籤在「回覆資訊」那一頁，每一列右邊按「標籤」。

改名不會弄亂已經分好的類。**刪掉標籤的話，掛在賓客身上的那一份也會一起拿掉**，
這個救不回來，刪之前想一下。

> 這一頁不是每場婚禮都有。之後要排桌次的話會很好用，跟我們說就幫你們打開。

---

## 悄悄話

賓客在**祝福牆**點「寫一封信」偷偷投給你們的話，全部在這裡。
一封一段，看得到記號、名字、時間跟內容，新的排在最前面。

名字或內容都可以搜尋，也可以「**匯出 CSV**」存起來。

> **只有你們讀得到**，賓客彼此看不到。
> 這一頁只能看跟匯出，不能改也不能刪 —— 別人寫給你們的話，就讓它保持原樣。

---

## 婚禮資訊

大廳（首頁）上的文字，都在這裡改。三個小分頁，上面還有一個開關。

### 最上面：開放桌次功能

打開之後，大廳的婚禮資訊最下面會出現「**尋找我的座位**」，導覽列也會多一個「桌次」。

**還沒到婚禮當天，建議先關著** —— 賓客就不會提早跑去找位子，
你們照樣可以在「桌次」分頁先把名單跟桌次圖慢慢準備好。當天早上再打開。

這個開關**改完馬上生效**，不用按儲存。

### 婚禮資訊

| 欄位 | 說明 |
|---|---|
| 標題 | 資訊卡最上面那行字，**最多 20 字**；留白就用你們兩個的名字 |
| 地點名稱 | 大字，例如「台北婚宴大飯店・二樓宴會廳」 |
| 地址 | 小字，也是「開啟地圖」按下去會搜尋的字 |
| 地圖連結 | 有自己想指定的地圖網址就貼這裡；留白就用地址自動開 Google 地圖 |
| 開始時間 | 幾點開始（日期由我們設定） |
| 交通・大眾運輸 | 捷運、公車、接駁怎麼搭，**可以放一張圖** |
| 交通・停車資訊 | 停車場在哪、能不能折抵，**可以放一張圖** |
| Dress Code | 想請大家穿什麼 |
| 關於禮金 | 想說的話 |
| 兩人的故事 | 大廳的故事區塊，邀請函那一頁也會用到 |
| 婚禮 hashtag | 最多 3 個，沒寫 `#` 會自動幫你補 |

> **留白的那幾塊，大廳上就不會出現**，不會留下一個空標題。
> 不想寫 Dress Code？留白就好，不用硬擠一句。

改完按「**儲存婚禮資訊**」。

### 當日流程

一列一個項目：時間 / 項目 / 說明。按「**＋ 加一列**」新增。

**由上到下就是賓客看到的順序，不會自己依時間重排** ——
所以「11:30 起」「儀式後」這種寫法完全沒問題。

說明可以留白。一列都沒填的話，大廳會顯示「流程稍後公布，敬請期待」。

改完按「**儲存流程**」。

### 自訂內容

首頁下面那一區的卡片，內建的（祝福牆、抽卡…）後面可以接你們自己的。
按「**新增自訂內容**」，選一種：

| 類型 | 點下去會 | 適合放 |
|---|---|---|
| **開啟連結** | 跳到你給的網址 | 直播連結、Google 相簿、共乘表單、社群 |
| **跳出說明** | 原地展開一段文字 | 接駁車時間、停車資訊、活動說明 |

填標題、一句話說明、內容，再給一個「排序」數字（小的排前面）。

---

## 桌次

賓客當天輸入名字就知道自己坐哪桌。兩個小分頁。

### 桌次圖

把飯店給的座位圖傳上來。可以一次選好幾張，或直接把圖**拖進虛線框**。

圖片會自動縮到適合手機看的大小，不用你先處理。

### 桌次搜尋及名單

**最上面的開關**：要不要讓賓客「輸入名字查桌次」。

關掉的話，賓客只會看到你上傳的桌次圖，沒有輸入名字的欄位 ——
名單還沒整理好、或本來就想讓大家自己看圖找位子，都很適合。
名單還在，之後打開就生效。

**匯入名單**：按「匯入名單」開視窗，一行一位賓客，逗號分開 ——
**直接從 Excel 複製貼上就可以**：

```
王小明, 第 3 桌
林美美, 第 3 桌, 素食
陳大同, 主桌
```

姓名跟桌次必填，第三欄備註選填。桌次寫「第 3 桌」或「玫瑰廳 A5」都行，
賓客看到的就是你寫的那樣。

匯入是**加上去**，不會蓋掉原本的名單。要重來就先按左下角「清空」。

> **賓客怎麼打都找得到。** 打「小明」找得到「王小明」，
> 打「王小明先生」也找得到「王小明」，空格跟全形半形都會自動處理。
> 查到之後還會列出同桌還有誰，一群朋友找位子比較方便。

**「同步現在的排桌」**：如果你們有用「排桌管理」，這顆按鈕會把排好的結果整份搬過來。
注意它會**整份換掉**這裡的名單（會先問你一次）。

---

## 排桌管理

> 這個功能要另外開，沒看到分頁就是還沒開，跟我們說一聲。

原本要用 Excel 排的那件事，整套搬進來了。三個小分頁。

### 排桌工作區

**左邊是「未安排」，右邊是一桌一桌。**

- **電腦**：直接把賓客的卡片**拖**到桌上。拖到哪就提示會放進第幾桌，
  會超過容量也會先跟你講。把一個人拖到另一個人身上就是**兩人交換位子**。
- **手機／平板**：每張卡片上有「**移動到桌位**」，按下去列出所有桌位（現在幾人、會不會超過），選一個就好。

最上面永遠看得到：同步狀態、**復原／重做**、儲存排桌，
接著是即時統計（總人數、已安排、未安排、超過容量…），再來是提醒。

**每一步都可以復原**，最多 60 步。放心亂拖。

**找人**：搜尋吃得到姓名、編號、桌號、標籤。
篩選可以依排桌狀態、RSVP、標籤（可多選）、桌號。
排序可以選「**優先按照標籤分組**」，未安排區就會一群一群分好。

> 一位賓客可能同時是「女方好友＋VIP＋素食」，分組時只能算一組。
> 按「**標籤權重**」排一下順序，由上往下第一個對到的就是他的組，不會有人重複出現。

**素食、行動不便、小孩、VIP 會自己浮出來**，直接寫在桌上，
上方提醒區也會列「還有 12 位沒安排」「第 05 桌超過 2 位」這類。
**這些只是提醒，不會擋住你** —— 婚宴當天本來就會臨時擠人。

### 桌位管理

新增、刪除、改桌名桌號容量類型。「**一次加好幾桌**」可以一口氣把 20 桌開好。

桌號一律顯示成兩位數（01、02…11），桌名選填。
**調整順序不會影響已經排好的賓客。**

### 匯入匯出

- **從出席表單匯入**：填過表單的賓客自動變成卡片，姓名、人數、葷素、標籤都跟著過來。
  不用先匯出成檔案再匯進來。
- **匯入 Excel／CSV**：給手上那份名單用（沒填表單的長輩、公司桌…）。
  分五步：選檔案 → 預覽 → 欄位對應 → 檢查資料 → 確認匯入。
  **有問題的那幾筆不會被匯進來**，會逐筆告訴你第幾列、哪裡不對。
- **匯出**：Excel（一個檔含「賓客明細」和「桌位排桌表」兩張表）或分開的 CSV。

### 排好了要按兩次

```
排桌（在畫面上拖，怎麼改都可以復原）
  → 按「儲存排桌」          ← 存起來，賓客還看不到
  → 按「同步至桌次查詢」      ← 賓客的「我的桌次」才會更新
```

**不會自動同步，這是故意的。** 排桌一定會反覆調整，
賓客那邊不該因為你還在整理就一直跳來跳去。
想先存起來明天再說，按「稍後再說」就好。

畫面最上面一直寫著現在的狀態：尚未同步 / 已同步（附時間）/ 有修改尚未同步。

> 同步完如果「**開放桌次功能**」還關著，系統會提醒你賓客目前還看不到 ——
> 這是婚禮當天最容易忘記的一步，看到這句就去「婚禮資訊」最上面打開它。

---

## 收禮小幫手

> 這個功能要另外開，沒看到分頁就是還沒開，跟我們說一聲。

婚宴當天在收禮台**收禮金、發禮餅**用的小工具。
它是**另一個網址**，幫忙的親友點連結、輸入通行碼就能用 ——
不用辦帳號、不用進後台，也**看不到出席回覆和悄悄話**。

### 連結與名單（婚禮前先做）

1. 按「**產生新連結**」，會拿到一組網址加一組六位數通行碼
2. 按「**複製連結＋通行碼**」，直接貼給幫忙收禮的親友
3. 匯入名單，兩種來源自己選：
   - **排桌名單**：帶得到編號、姓名、桌次、人數 —— 有用排桌管理的話最推薦，核對最快
   - **出席回覆**：帶得到姓名、人數、關係、備註

> **通常一場婚禮只要一組連結。** 四五個人共用同一組，統計才會加在一起。

> 名單是**匯過去的一份快照**，不是即時連動的 ——
> 當天收禮台要的是「現在這一版」，不該因為你在休息室調了一下桌次就跟著跳。
> 名單改了就再按一次匯入，**已經記好的收禮不會受影響**。

### 親友那邊會看到什麼

上面三個分頁：

- **收禮台**：賓客名單。搜尋名字、編號、桌次，點一下就開記帳表單
- **紀錄**：所有人記過的每一筆，可以改、可以刪
- **統計**：總額、盒數、到場人數

記帳表單就問那幾件事：**禮金金額**（一排常見金額點一下就填好）、
**禮餅發了沒**、**幾盒**、**幾人**、**備註**。名單上沒有的人按「＋ 名單外」直接記。

每個人第一次用會被問一次名字，之後每一筆都會記下是誰收的。

> **好幾個人同時記不會打架**，會場網路很爛也沒關係 ——
> 斷線時照樣按得下儲存，連上就自己送出去。

### 收禮統計（你們看的）

禮金總額、禮餅盒數、到場人數、還有幾位沒收到，
還有「**誰記了多少**」—— 事後對帳時，哪位親友手上該有多少現金，一看就知道。

**數字是即時的。** 阿華在收禮台記的那一筆，你手機上當下就看得到。

明細可以「匯出 CSV」。

### 婚禮結束後

到「連結與名單」按一下「**停用**」。
拿著連結的人就進不去了，但紀錄跟統計都留著，隨時可以再啟用。

> 連結跟通行碼要當鑰匙看待，傳給幫忙的親友就好，不要貼在大群組裡。
> 結束後順手停用，很花不了三秒。

---

## 感謝信

你們寫的信，賓客輸入自己的名字（或只有你們懂的暗號）就能拆開來看。
畫面是一個信封，輸入正確後封蠟散開、信紙滑出來，
下面還可以「儲存下載」把信存成一張圖收進手機相簿。

按「**寫一封信**」：

| 欄位 | 說明 |
|---|---|
| 專屬詞彙 | 賓客要輸入的通關密語，逗號分開可以寫好幾個 |
| 信的標題 | 信紙最上面那行 |
| 信的內容 | 最多 2000 字，換行會保留 |
| 署名 | 留白就用你們的名字 |
| 設為通用信 | 沒對到任何詞彙的人就領這一封 |

**通用信可以寫好幾封**（給同事一封、給遠道而來的親友一封…），
系統會依名字挑一封 —— 同一個名字每次拿到的都是同一封，不會這次那次不一樣。

清單上面可以切「全部 / 通用信 / 指定信」。

> **專屬詞彙不要寫太短。** 比對允許互相包含，單字詞很容易被別人誤中。
> 建議至少兩個字，最保險是直接寫全名。

> **這裡不適合放不能被別人看到的祕密。** 信件內容技術上是公開可讀的，
> 適合寫給某個人的祝福，不適合寫真的不能外流的話。
> 那種內容請走「悄悄話」（只有你們讀得到的那個）。

---

## 婚禮小卡

抽卡頁的卡池。賓客抽到會收進自己的收藏，點開能放大、能存下來。

選好照片會**先跳出裁切框**：拖曳移動、兩指縮放，
框裡看到的就是最後的樣子（卡片是直式 2:3）。
一次可以選很多張，會一張一張輪流裁切；不想要那一張就按「取消」跳過。

裁好之後每張卡可以改：

- **卡名**：顯示在卡片下方，預設用檔名
- **等級**：`SSR` / `SR` 抽到時有彩虹光膜跟煙火，`R` / `N` 是一般卡
- **說明**：選填

**改完離開欄位就自動存檔**，不用另外按儲存。想重切構圖按「重新裁切」。

> **每張卡被抽到的機率都一樣。** 想讓稀有卡難抽，就少放幾張 SSR、多放幾張 N。

> 賓客一進抽卡頁就會把整個卡池載下來，**建議控制在 30 張以內**，才不會等太久。

---

## 新人故事牆

「我們的故事」那條橫向時間軸。兩種東西：

| 型態 | 是什麼 | 要填 |
|---|---|---|
| **故事** | 時間軸上的一張拍立得 | 照片（可以先留空）、標題、年份、時間補充、章節、描述 |
| **章節** | 分隔段落，例如「第一幕・我們的相遇」 | 名稱、副標 |

兩種都用「**排序**」決定先後，數字小的排前面 ——
章節的數字要比它底下那些故事小。

照片一樣會跳出裁切框，比例可以選直式 3:4 / 方形 1:1 / 橫式 4:3。
**沒有照片也存得起來**，可以先把文字寫完，照片之後再補。

第一次打開這一頁時，裡面已經有一份現成的故事牆當起點 ——
直接改文字、換照片，或整個刪掉重來都可以。

---

## 新人熟悉測驗

「看你多了解我們」的題目。賓客一次看到所有題目，
全部答完才送得出去，送出後看到自己的分數跟每題的長條圖。

按「**新增題目**」：

- **題型**：單選（選完自動跳下一題）／ 複選（**全對才得分**）
- **題目**：最多 60 字
- **四個選項**：固定四個，每個最多 40 字
- **正確答案**：在選項左邊勾起來

順序用清單左邊的 **⠿** 拖曳調整，編號會自動重排。**最多 50 題。**

> **你們不用自己作答**，答案在這裡就設好了。

> 一開始就有三題現成的，直接改就好。

### 作答記錄

看得到作答人數跟平均分數。
「**清空所有作答紀錄**」只會刪掉票，**題目不會被動到** ——
測試完想歸零、或婚禮當天想重新開始統計，就按它。

---

## 賓客那邊會看到什麼

| 頁面 | 內容 | 你在哪裡改 |
|---|---|---|
| 首頁 | 婚禮資訊、當日流程、Dress Code、交通、故事、倒數、卡片區 | 婚禮資訊 |
| 出席回覆 | 邀請函＋表單 | 出席回覆 → 表單設定 |
| 我的桌次 | 輸入名字查桌次＋桌次圖 | 桌次 / 排桌管理 |
| 祝福牆 | 賓客留言，還有投悄悄話的信箱 | （賓客自己寫，你在「悄悄話」讀） |
| 給你的信 | 拆信封看你們寫的信 | 感謝信 |
| 我們的故事 | 橫向時間軸 | 新人故事牆 |
| 看你多了解我們 | 測驗 | 新人熟悉測驗 |
| 抽卡 | 抽小卡、收藏 | 婚禮小卡 |
| 集氣送祝福 | 甜點桌集氣 | （不用設定） |

沒開的頁面不會出現在導覽列上，賓客也點不到。
想開／想關哪一頁，跟我們說。

---

## 婚禮前・當天・結束後

不用一次做完。這是我們建議的節奏：

### 拿到後台的第一週

- [ ] 兩個人的手機都**把後台加到主畫面**
- [ ] 填「**婚禮資訊**」：地點、地址、開始時間、交通
- [ ] 確認「**表單設定**」問的題目是你們要的
- [ ] 把**邀請函網址**發出去（後台「表單設定 → 查看表單」那一頁）

### 有空的時候慢慢做

- [ ] 「**當日流程**」
- [ ] 「**兩人的故事**」跟「**新人故事牆**」
- [ ] 「**新人熟悉測驗**」的題目
- [ ] 「**婚禮小卡**」挑照片（30 張以內）
- [ ] 「**感謝信**」寫給重要的人

### 婚禮前一到兩週

- [ ] 「**出席回覆 → 總覽**」看素食幾份、兒童椅幾張，回報飯店
- [ ] 「**排桌管理**」排桌（或「桌次」直接匯入名單）
- [ ] 排好後按「**儲存排桌**」→「**同步至桌次查詢**」
- [ ] 上傳「**桌次圖**」
- [ ] 「**收禮小幫手 → 連結與名單**」產生連結、匯入名單

### 婚禮前一天

- [ ] 把**收禮連結＋通行碼**傳給幫忙的親友，請他們先點開試一次
- [ ] 桌次有異動的話，記得重新同步、重新匯入收禮名單

### 婚禮當天早上

- [ ] 「**婚禮資訊**」最上面把「**開放桌次功能**」打開
- [ ] 手機主畫面點開後台，停在「**收禮統計**」

### 結束後

- [ ] 「**收禮小幫手**」匯出 CSV 存一份，然後按「**停用**」
- [ ] 「**悄悄話**」讀完，匯出存一份
- [ ] 「**出席回覆**」匯出 CSV 存一份
- [ ] 「**新人熟悉測驗 → 作答記錄**」想留就留著

> 網站不會突然關掉，慢慢看沒關係。但匯出來存一份比較安心。

---

## 想改但改不動的東西

有幾個欄位後台是鎖著的，不是壞掉：

| 改不動的 | 為什麼 |
|---|---|
| 你們的名字 | 全站到處都在用 |
| 婚禮日期 | 倒數計時跟很多地方都靠它 |
| 網址（代號） | 改了已經發出去的連結會全部失效 |
| 要開哪些頁面 | 影響導覽列跟權限 |
| 入場登入的開關 | 同上 |
| 出席回覆的截止時間 | 同上 |
| 邀請函上的照片集 | 照片放在素材資料夾，後台不能上傳 |

這些跟我們說一聲就幫你們改，通常當天就好。

---

## 常見問題

**改完賓客看不到？**
請他重新整理一次。還是不行的話，確認你有按到那一頁的「儲存」按鈕。

**手機上怎麼切分頁？**
按左上角的「≡」打開選單。

**不小心刪掉東西了？**
排桌管理裡的動作可以「復原」（最多 60 步）。
其他分頁刪掉的內容救不回來 —— 所以刪之前系統都會問你一次，看清楚再按。

**兩個人同時在後台改會怎樣？**
同一個欄位同時改，後按儲存的那個會蓋掉先按的。分頭改不同分頁就完全沒問題。

**後台網址不小心傳出去了？**
不用緊張。真正的門是 Google 帳號 —— 不在名單內的人打開那一頁，
連一個字都改不了、也看不到回覆內容。

**收禮的連結傳錯人了？**
到「收禮小幫手 → 連結與名單」把那一組「停用」，再產生一組新的。

**照片傳不上去？**
檢查是不是 JPG / PNG / WebP。太大的圖系統會自動壓縮，不用你先處理。

**還是卡住？**
截個圖傳給我們，講一下你在哪一頁、按了什麼。不用先自己研究，我們處理比較快。

---

祝你們一切順利。婚禮當天記得吃東西。

**みなと製作所 Minato Studio**
